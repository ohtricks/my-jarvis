"""Ferramenta de calendário — placeholder para Fase 4 (Google Calendar API)."""


class CalendarTool:
    def get_events(self, date: str = "hoje") -> str:
        return (
            "Integração com Google Calendar ainda não configurada. "
            "Será implementada na Fase 4 do projeto."
        )

    def add_event(self, title: str, date: str, time: str = "") -> str:
        return (
            "Integração com Google Calendar ainda não configurada. "
            "Será implementada na Fase 4 do projeto."
        )
